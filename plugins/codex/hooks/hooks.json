{
  "description": "Deterministic skill-load nudge: when a user prompt names codex, remind Claude to invoke the codex skill before driving the codex CLI.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/skill-nudge.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
